import React from "react";
import FormHelperText, {
  FormHelperTextProps,
} from "@mui/material/FormHelperText";

export default function MuiFormHelperText(
  props: FormHelperTextProps
) {
  return <FormHelperText {...props} />;
}
